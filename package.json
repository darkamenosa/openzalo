{
  "name": "@openclaw/openzalo",
  "version": "2026.2.18",
  "private": true,
  "description": "OpenClaw OpenZalo channel plugin (personal account via openzca CLI)",
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "tsx": "^4.20.5"
  },
  "openclaw": {
    "extensions": [
      "./index.ts"
    ]
  }
}
